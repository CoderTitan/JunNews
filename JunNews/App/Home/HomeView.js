/*
 * @providesModule HomeView
 *
 * */
import React, { Component } from 'react';
import {
    Platform,
    StyleSheet,
    Text,
    View
} from 'react-native';
import Detail from 'Detail'

export default class HomeView extends Component {
    render(){
        return(
            <View style={{backgroundColor:'#ededed', flex:1, justifyContent:'center', alignItems:'center'}}>

            </View>
        )
    }
}